#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always 0 if succesful
 */

int main(void)
{
int nums;

for (nums = 0; nums < 10; nums++)
{
printf("%d", nums);
}
putchar('\n');
return (0);

}
